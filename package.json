{
  "name": "server",
  "version": "0.0.1",
  "description": "This contains the code for an Express server run with Node.js to outline the endpoints needed for basic functionality of our app's RESTful API.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yejia Zhang",
  "license": "ISC",
  "dependencies": {
    "express": "*",
    "mongoose": "*"
  }
}
